# 📝 Simple To-Do App

A basic To-Do List web app built with PHP, MySQL, HTML, CSS, and JavaScript.

## Features
- Add, edit, and delete tasks
- Mark tasks as complete
- Uses MySQL for persistent storage

## Setup
1. Import `todo_db.sql` into phpMyAdmin.
2. Update `db.php` with your database credentials.
3. Run using XAMPP/WAMP: http://localhost/todo-app
